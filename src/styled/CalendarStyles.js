import styled from 'styled-components';

// Styled container for the calendar page
export const CalendarContainer = styled.div`
  height: 500px;
  margin: 50px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  background-color: #ffffff;
  border-radius: 8px;
`;

// Example of a styled title if needed
export const CalendarTitle = styled.h1`
  font-size: 24px;
  color: #333;
  text-align: center;
  margin-bottom: 20px;
`;
